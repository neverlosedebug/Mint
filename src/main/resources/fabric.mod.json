{
  "schemaVersion": 1,
  "id": "mint",
  "version": "${version}",
  "name": "Mint",
  "description": "A utility mod for 1.21.8",
  "environment": "client",
  "accessWidener" : "mint.accesswidener",
  "icon": "assets/mint/icon/icon.png",
  "entrypoints": {
    "client": [
      "net.mint.Mint"
    ]
  },
  "mixins": [
    "mint.mixins.json"
  ],
  "depends": {
    "fabricloader": ">=${loader_version}",
    "fabric": "*",
    "minecraft": "${minecraft_version}"
  }
}